apiVersion: v1
kind: Service
metadata:
  name: recommender-service
spec:
  type: NodePort
  selector:
    app: recommender
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007